{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Default to development configuration for IDE support
    "noEmit": true,
    "incremental": true,
    "tsBuildInfoFile": "./.tsbuildinfo",

    // Less strict for development
    "noUnusedLocals": false,
    "noUnusedParameters": false
  }
}
